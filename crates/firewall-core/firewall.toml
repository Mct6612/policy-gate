# firewall.toml — Local configuration for policy-gate
allow_anonymous_tenants = true

[[rule_exceptions]]
rule_id = "RE-004"
regex = "(?!.*print\\(sys\\.argv\\))"
reason = "Operator-Review: Rule Exception for Cluster"
