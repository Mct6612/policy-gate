# policy-gate Configuration
# Active Mode: BERT Semantic Engine

engine_mode = "bert"
model_path = "models/all-MiniLM-L6-v2.onnx"
tokenizer_path = "models/tokenizer.json"
semantic_threshold = 0.70
semantic_enforce_threshold = 0.95
